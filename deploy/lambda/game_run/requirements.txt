# Dependencies for game_run Lambda (bundle with src). boto3 is in Lambda runtime.
# Install for Lambda Linux: use --platform manylinux2014_x86_64 when building on Windows.
python-chess>=1.999
python-dotenv>=1.0.0
pydantic>=2.0.0
openai>=1.0.0
google-genai>=1.0.0
anthropic>=0.40.0
mistralai>=1.0.0
cohere>=5.0.0
groq>=0.4.0
